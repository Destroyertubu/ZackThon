# 知野 ZHIYE：架构设计与完整项目

已将前端、后端、数据库、三维场景、知识组合、存档、个人画布、公开路线、测试和部署文件打包。

**[下载完整项目 ZIP](sandbox:/mnt/data/zhiye-complete-project.zip)**　约 3.37 MiB，包含 56 个文件。

**[下载完整架构设计文档](sandbox:/mnt/data/知野_架构与实现说明.md)**　｜　**[查看测试结果与交付边界](sandbox:/mnt/data/知野_测试与交付边界.md)**

**运行状态：默认使用明确标记的原创演示内容，核心交互与后端已测试。真实知乎鉴权、官方 beta 响应契约，以及 Three.js GPU 渲染路径仍需联调。** 下图是项目实际运行截图，使用已经验证的软件三维兼容模式。

![知野实际运行界面：软件三维兼容模式](sandbox:/mnt/data/zhiye-preview.png)

## 一、架构选择：三维负责探索，二维负责阅读与思考

这份实现采用：

**Three.js + 原生 JavaScript 模块 + DOM/CSS + SVG + FastAPI + SQLite。**

没有把长篇中文正文、输入框和所有操作都塞进三维空间。场景负责方位、距离、接近和关系；稳定的二维面板负责阅读、编辑、知识组合和来源核对。

```text
                         浏览器
┌─────────────────────────────────────────────────┐
│ 三维世界：相机、移动、岛屿、桥梁、话题标签、锚点  │
│     Three.js 主渲染 / 软件三维兼容渲染            │
├─────────────────────────────────────────────────┤
│ 阅读面板 │ 知识行囊 │ 想法编辑 │ SVG 心路画布     │
├─────────────────────────────────────────────────┤
│            旅程状态 + IndexedDB 本地副本         │
└──────────────────────┬──────────────────────────┘
                       │ 同源 API，前端没有开发者密钥
┌──────────────────────▼──────────────────────────┐
│                    FastAPI                      │
│ 会话与权限 │ 世界生成 │ 内容检索 │ 存档与公开匹配 │
├─────────────────────────────────────────────────┤
│ 缓存 → 原子预算预留 → 合并重复请求 → 内容适配器  │
└──────────────┬───────────────────┬───────────────┘
               │                   │
        SQLite / WAL          知乎官方搜索
   私有记录、缓存、配额      服务端 Bearer 鉴权
```

### 为什么选择这套三维实现

主渲染器使用 Three.js 的 WebGL2 路径，便于直接控制相机、几何、材质、光照和渲染资源。项目固定依赖 `three@0.180.0`，这是实现版本选择，不宣称它是当前最新版。([Three.js](https://threejs.org/docs/pages/WebGLRenderer.html))

场景中的重复树木使用 `InstancedMesh`，而不是每棵树单独创建一套绘制对象；静态地形与桥面也做了几何合并。实例化适用于复用相同几何和材质、但位置不同的对象，是 Three.js 官方明确提供的减少绘制调用的方法。([Three.js](https://threejs.org/docs/pages/InstancedMesh.html))

增强渲染器源码中包含程序化天空、水面着色、阴影，以及通过 `EffectComposer` 组织的渲染、泛光与输出处理。**这些 GPU 效果的源码已交付，但本次没有完成目标设备实测，不能据此承诺帧率。**([Three.js](https://threejs.org/docs/pages/EffectComposer.html))

后端使用 FastAPI 与 Pydantic 做接口和输入校验，并提供本地 OpenAPI 文档页；数据存储则选择便于黑客松部署的 SQLite，而不是先引入一整套分布式基础设施。FastAPI 本身基于 Python 类型声明组织参数校验和 OpenAPI 契约。([PyPI](https://pypi.org/project/fastapi/0.128.2/))

## 二、已经实现的产品功能

| 模块                 | 当前代码中的实现                                             |
| -------------------- | ------------------------------------------------------------ |
| **问题即世界种子**   | 输入问题创建世界；相同种子的布局稳定。当前使用规则提出探索方向，不冒充已经建好的全站语义图。 |
| **第一人称探索**     | WASD、方向键、Shift 加速、鼠标视角、指针锁定与释放、岛屿和桥梁范围碰撞。 |
| **靠近话题浮现内容** | 驻留后加载内容卡片；可以点击阅读、查看来源或按 E 收纳，不按移动帧反复搜索。 |
| **增量词云扩展**     | 提供“深入一层、换个角度、跨界试探”三类方向；新增节点不会移动旧节点。 |
| **知识行囊**         | 收纳、去重、个人备注、选择 2–4 张卡片组合，保留完整来源关系。 |
| **想法锚点**         | 记录位置、话题、文字与时间；默认私人；申请公开后进入待审核状态。 |
| **个人画布**         | 显示探索路线、话题、想法及真正连接来源话题的知识桥梁；支持缩放、拖动和地图直达。 |
| **旅程存档**         | 本地副本、服务器存储、自动与手动保存、恢复、删除、Markdown/JSON 导出及修订冲突检测。 |
| **同频的人**         | 根据双方主动公开的路线匹配；支持查看公开路径、更新快照及撤回。没有预置虚构用户。 |
| **舒适性设置**       | 减少动态效果、放大正文、增强对比度、轻量画质、触屏入口；打开面板时暂停人物移动。 |

当前世界设置了 **64 个话题节点、深度 3** 的原型边界，避免演示阶段陷入无界扩图与无限接口调用。

### 知识行囊不是“换皮收藏夹”

组合流程已经落实为：

```text
选择 2–4 张来源卡片
          ↓
选择联系：补充 / 分歧 / 类比 / 因果假设
          ↓
写下自己的解释
          ↓
生成：新问题 / 观点草稿 / 行动实验
          ↓
保留原始来源，并在画布上形成知识桥梁
```

例如，把关于“热爱”的卡片与“反复实践”的卡片组合，写下：

> 热爱也许不是行动的前提，而是在反复实践中逐渐形成的。

系统会把这段理解组织为草稿或行动实验，并加入适用条件、观察记录或反证问题，**不会把它改写成“已经证明的事实”**。

本版采用的是**本地规则与用户解释的组合**，没有伪称调用了尚未接通的 AI。派生卡保留 `sourceIds`，后续继续组合时仍能回溯原始来源。

个人画布中的紫色连线来自这些真实关系，而不是装饰线：

**[查看个人画布实际截图](sandbox:/mnt/data/zhiye-canvas.png)**

## 三、可视化与素材：统一风格，而不是堆砌高精度资源

这版采用“**雾海里的知识群岛**”：

远处是低饱和青绿的岛屿和话题；近处是门环、灯光与少量内容卡；停下来阅读时，界面切换为奶油色纸面。金色表示走过的路，紫色表示自己组合出的知识联系。

**实际交付的场景素材来自原创程序化代码**，包含岛屿、岩石、石桥、植被、门环、灯光、天空和水面。它们不依赖外部图片地址，也不需要先下载大型模型包才能体验原型。

中文标签采用 DOM 投影，并做距离过滤、显示数量限制和重叠剔除；正文保留在二维面板中。手机首页也专门避开了三维标签与主标题重叠的问题。

关于你提供的素材信息，边界如下：

| 素材                               | 实际状态                                                     |
| ---------------------------------- | ------------------------------------------------------------ |
| 原创群岛场景、图标和环境音合成代码 | 已包含                                                       |
| 刘看山三视图、动态素材             | 你提供了名称，但没有文件或可下载地址，因此没有包含，也没有用其他模型冒充 |
| Poly Haven HDRI/PBR 素材           | 在素材台账中列为可选升级方向，当前未下载、未接入             |
| 字体文件                           | 不包含；使用系统字体回退                                     |

Poly Haven 的资产许可声明为 CC0，可作为后续外部素材来源，但这不意味着其网页图片、Logo 或其他品牌素材也具有相同授权。项目文档区分了“已提供素材”和“可选升级素材”。([Poly Haven](https://polyhaven.com/license))

## 四、知乎接口：哪些已实现，哪些仍需核验

我核实了官方公开文档中的搜索请求方式：使用服务端 `Authorization: Bearer ...`，并传入**秒级** `X-Request-Timestamp`。代码中的 HTTP 搜索适配按这一请求定义实现。([知乎数据开放平台](https://developer.zhihu.com/))

不过，**你给出的官方 Skill ZIP 在本次环境中没有下载成功，也没有真实 Access Secret，因此不能把适配代码说成已经完成官方联调。**

项目为这个接入阶段准备了两个脚本：

```bash
# 下载用户提供的官方 Skill 包；检查解压路径与体积，不自动执行包内代码
python scripts/fetch_official_skill.py

# 核验 Skill、配置服务端凭据后，显式进行一次真实搜索检查
python scripts/probe_zhihu.py --live --query "如何发现自己的兴趣"
```

真实响应的字段映射集中在内容适配层。`schema.example.json` 明确标注为**映射格式示例，而非已核验的 beta 契约**。字段不匹配、鉴权失败或没有有效来源链接时，系统报错，不用演示结果冒充真实内容。

### 内容可信性和配额控制已经写进代码

真实搜索结果始终标为“**知乎搜索摘要**”。没有全文依据时，不把它包装为逐字“金句”；阅读面板提供实际来源链接，也不会补写一篇并不存在的完整文章。

缓存和额度放在后端：按你提供的比赛说明，默认设置开发者搜索总预算为 5000 次/日；另设每个访客 80 次实时未命中预算，这是应用自己的公平使用限制。相同查询的并发请求合并，已有缓存直接复用。

**本版没有接通知乎 OAuth、关注流、故事、知识、热榜或直答 Agent。** 当前社交是本应用访客之间的公开路线匹配，不是假装取得了知乎用户的关注关系；访客 Cookie 也不能冒充知乎登录。

## 五、启动方式

解压后进入 `zhiye` 目录。macOS/Linux：

```bash
python -m venv .venv
source .venv/bin/activate

python -m pip install -r requirements.txt
npm install

cp .env.example .env
python run.py
```

浏览器打开：

```text
http://127.0.0.1:8000
```

Windows 命令、Docker 启动、配置项、接口说明和完整操作流程都在压缩包内的 `README.md`。

**`npm install` 暂时不可用时，可以跳过这一项，使用软件三维兼容模式。** 安装 Three.js 成功后重启服务器，再检查增强渲染路径。

建议第一轮按这个顺序体验：

**开始漫游 → 收纳两张卡 → 写一个想法锚点 → 组合出新问题 → 查看画布中的知识桥梁 → 保存旅程。**

## 六、验证结果

交付包重新解压后，测试再次通过：

| 验证范围                    | 结果                                                         |
| --------------------------- | ------------------------------------------------------------ |
| JavaScript 领域测试         | **10 / 10 通过**                                             |
| Python / FastAPI 测试       | **14 / 14 通过**                                             |
| JavaScript 语法检查         | 通过                                                         |
| 独立 Uvicorn + 真实回环 TCP | 启动、静态资源、会话连续请求、世界与内容接口、跨源拒绝检查通过 |
| Chromium 交互检查           | WASD、收纳、组合、锚点、知识桥梁、面板暂停移动、服务器保存通过 |
| 手机首页                    | 390 × 844 布局检查通过，无水平溢出                           |

浏览器交互检查使用了软件三维渲染器、ASGI 接口桥接和模拟本地存储，**不等于已经验证真实 IndexedDB、浏览器网络层会话或 Three.js GPU 效果**。这些边界和复现命令都写在测试报告中。

这份工程已经可以运行和继续开发；正式参赛前，优先完成的是 **官方 Skill 与真实响应核验、知乎 OAuth，以及演示设备上的 Three.js 主渲染验收**。完整设计、接口接入步骤和素材台账分别位于项目的 `docs/` 目录。