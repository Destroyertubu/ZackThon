# 新房间实机交付状态 · 2026-09-10

新版已在独立 worktree 接入并启动，默认载入完整房间。可作为本地运行和继续精修的版本；**不是87项资产、28项穿模及全部美术验收通过的交付**。分支 codex/wanderwise-production-room，原版基线2c80d9bf保留。

## 已实现

完整连通壳体/露台、阅读软椅/布毯/书架、六抽屉日志桌、三联玻璃柜、中央四槽合成台、电话亭/座机、壁炉、灯冠和12灯笼、96本书、4垂藤、纸杯等陈设、几何山体/林谷/河道。参考图仅指导设计，没有充当场景背景。保留无准星、无简化阅读模式，以及原第三人称控制、业务、SQLite和接口。

自制可编辑full.blend、按部件制作的Python脚本、Meshopt压缩GLB和同源解码器均已提供。一个Canvas、一个Three.js渲染器和一个主循环；一盏投影主光、四盏不投影补光，没有Bloom。保存原小屋的互斥回滚入口。

## 实际验证

Chrome真实浏览器完成19项链路检查：五个空间左键入口、中文输入隔离、路线/正文/收藏去重/锚点/手工合成/洞察保存/回家刷新、隔墙阻挡、低画质、失焦暂停、10次实际家园/世界往返、1203.6秒持续运行。没有页面JavaScript异常。测试操作使用独立访客及临时数据库，未读取或迁移真实用户存档。

发现并修复东西墙COL代理旋转丢失；四墙实际正走、绕台路线、北栏跳跃阻挡和36个贴墙镜头方向复测通过。12固定视角与出生、设施、低画质截图均来自运行中的应用，CAM07为明确标记的QA剖切视角，不是日常镜头。

GLB失败/重试、嵌入贴图失败、真实WebGL上下文丢失、迟到加载取消、家具内存档安全复位与10次独立引擎生命周期也有单独故障测试。持续测试加载的版本早于最后的InstancedMesh显式dispose及QA剖切背景修正；最后修正另以故障/生命周期测试和固定机位截图复验，未把它写成重新跑过20分钟。

新房间4项资产/碰撞单测、6项旧版回滚夹具、3项物理和3项漫游交互测试通过；25个前端模块语法检查通过。旧版夹具的三角预算断言只证明回滚资产，不适用于新版。

## 性能实测及范围

| 条件/指标 | 结果 |
|---|---|
| 设备、系统 | Apple M5 / macOS26.6.2 arm64 |
| 浏览器 | Chrome152.0.7977.83，headed，ANGLE Metal |
| 画面 | 1920×1080，DPR1，中画质 |
| 网络与缓存 | localhost HTTP，无限速，新浏览器上下文；OS文件缓存未清空 |
| 本地首屏可操作 | 1.326秒；不代表公网冷启动 |
| 启动子资源传输 | 11,672,804字节，HTML另见startup-budget.json；本地未启用HTTP gzip |
| 持续采样 | 71,889个原始可见帧，包含切场 |
| 中位FPS / P95 | 59.88 FPS / 17.5ms |
| 低画质短采样 | 59.9 FPS / P95 17.0ms；不是另一轮20分钟测试 |
| 绘制预算 | 常见约253–279 calls；**未达到≤150目标** |
| 三角计数 | 约47.6–50.5万/帧，含阴影等pass和实例；不能据此宣称可见面≤20万达标 |

10次相同往返检查点均为149 geometry / 38 texture；后续移动使标签从3增至4，对应151/40增至152/41，随后稳定。未见持续增长，但此结论只涵盖本设备、测试时长和计数器观察，不声称排除所有GPU/驱动泄漏。

## 未验证或未完成

仍有明显参考图质感差距，材质样板最终美术批准未完成。动态门开合/内外穿行、其余三条椅背毯、三档LOD、烘焙光照、部分植物/道具/音频未完成。人物保持原1.5米契约，未改为制作包1.72米；物理仍使用原受限dt，没有声称固定补步完成。28项接触用例中未测/部分覆盖项逐一列于reports/production-collision/28-cases.md，不能标28/28。

真实知乎、付费AI、公网HTTPS/线上发布未验证，本轮没有调用、购买、push或部署。启动压缩包已在解压目录使用已安装Python依赖验证HTTP和数据库健康；首次联网pip安装、Windows和其它GPU未复验。

## 证据与来源

短漫游录像：reports/full-browser/home-walkthrough.webm，实际浏览器键鼠操作，1280×720；不是离线渲染。

主要记录：reports/production-acceptance/home-acceptance.json、raw-frames.json.gz、failure-checks.json、release-http.json；reports/production-collision/results.json；reports/full-browser/CAM01至CAM12.png。

来源和SHA256见reports/production-assets/asset-receipts.json。glTF基础校验无errors/warnings，但validator不支持Meshopt扩展内部验证，实际浏览器解码另验。新增模型和纹理自制，无新增第三方付费包、私有原始文件或凭证。
